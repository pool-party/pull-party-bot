language: java
script:
  - ./gradlew check
before_cache:
  - rm -f $HOME/.gradle/caches/modules-2/modules-2.lock
  - rm -fr $HOME/.gradle/caches/*/plugin-resolution/
cache:
  directories:
    - .gradle
    - build
    - $HOME/.gradle/wrapper
    - $HOME/.gradle/caches
    - $HOME/.gradle
